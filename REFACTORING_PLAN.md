# План рефакторинга

## Текущее состояние

- Тип проекта: одностраничное приложение мессенджера.
- Стек: TypeScript, собственные `Block`/`Router`/`Store`, собственный шаблонизатор, Webpack, Express.
- Последняя проверка: `npm.cmd run lint`, `npm.cmd run build` проходят.
- В рабочем дереве есть незакоммиченные изменения, поэтому перед крупными структурными шагами стоит сделать контрольный коммит.

## Сделано

- [x] Исправлен README: файл приведен к читаемому UTF-8, описание обновлено под текущий Webpack-проект.
- [x] Убран устаревший Parcel-конфиг `.parcelrc`.
- [x] Удалены Parcel-зависимости `@parcel/config-default` и `parcel-bundler` из `package.json` и `package-lock.json`.
- [x] Обновлен CI workflow под текущий Webpack-проект.
- [x] Webpack output переведен с `[hash]` на `[contenthash]`.
- [x] Убрана скрытая зависимость Webpack от `process/browser`; переменные окружения теперь подставляются через `EnvironmentPlugin`.
- [x] Исправлены несколько битых русских строк в чатовых toast/UI-сообщениях.
- [x] Исправлен favicon path в `src/static/index.html`.
- [x] CSP вынесен в общий `src/config/csp.js`; HTML и Express используют один источник политики.
- [x] Production CSP ужесточен: `unsafe-eval` оставлен только для dev-сборки, `unsafe-inline` убран.
- [x] Webpack разделен на `webpack.common.js`, `webpack.dev.js` и `webpack.prod.js`; npm-скрипты указывают нужный конфиг явно.
- [x] Проведен `npm audit`; безопасный `npm audit fix` не смог закрыть оставшиеся уязвимости без breaking upgrades.
- [x] Исправлены оставшиеся найденные mojibake-строки в auth/error-страницах, validation/error-сообщениях и комментариях Auth API.
- [x] Проверки после изменений проходят: lint и production build.
- [x] Продакшен-зависимости обновлены до состояния без уязвимостей по `npm audit --omit=dev`: Express обновлен в ветке 4.x, runtime-зависимость `uuid` удалена.
- [x] Убраны неиспользуемые устаревшие Babel-зависимости (`babel-cli`, `babel-eslint`, `babel-polyfill`, `regenerator-runtime`), обновлены Babel/Webpack/Mocha/jsdom/PostCSS dev-зависимости.
- [x] `src/assets/img/chat-bg.png` заменен оптимизированным `chat-bg.jpg`; production build больше не предупреждает о превышении рекомендуемого размера ассета.
- [x] TypeScript обновлен до актуальной ветки 5.x, добавлен `npm run typecheck`, включены дополнительные строгие флаги компилятора: `alwaysStrict`, `exactOptionalPropertyTypes`, `noImplicitThis`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `strictBindCallApply`, `forceConsistentCasingInFileNames`.
- [x] Обновлены ESLint/TypeScript ESLint и Stylelint до совместимых современных версий без перехода на ESLint 9 flat config; CI переведен на Node 20 и запускает typecheck.

## Осталось срочно

- [x] Проверить весь репозиторий на оставшиеся проблемы кодировки в русских строках, комментариях, шаблонах и UI-лейблах.
- [x] Принять решение по CSP: оставить политику в HTML, в Express или вынести в общий источник конфигурации.
- [x] Убрать или обосновать `unsafe-inline` и `unsafe-eval` в CSP.
- [x] Разделить Webpack-конфигурацию для development и production.
- [x] Проверить `npm audit` и отдельно решить, какие уязвимости можно закрыть без breaking changes.
- [x] Спланировать breaking upgrades для оставшихся уязвимостей в зависимостях: остаток находится только в dev-зависимостях `mocha` и `webpack-dev-server`/`sockjs`; prod-аудит чистый.
- [x] Оптимизировать `src/assets/img/chat-bg.png`: production build предупреждает, что ассет превышает рекомендуемый лимит Webpack.
- [ ] Сделать чистый контрольный коммит перед более крупным рефакторингом.

## Дорожная карта

### 1. Стабилизация

- [x] Привести исходные файлы к UTF-8.
- [x] Обновить документацию проекта.
- [x] Почистить устаревшие Parcel-зависимости и конфиги.
- [x] Сделать npm/CI-проверки предсказуемыми для текущего Webpack-проекта.
- [x] Разделить build-конфигурацию для разработки и продакшена.
- [ ] Создать чистый базовый коммит перед структурными изменениями.

### 2. Модернизация инструментов

- [x] Выбрать путь: оставить Webpack и модернизировать конфиг или перейти на Vite.
- [x] Обновить TypeScript и включить более строгие настройки компилятора.
- [x] Обновить конфиги ESLint и Stylelint до актуального формата.
- [ ] Довести код до полного `strict`/`noUncheckedIndexedAccess` после типизации форм, store и шаблонизатора.

### 3. API и состояние приложения

- [ ] Заменить `XMLHttpRequest`-транспорт типизированным `fetch`-клиентом.
- [ ] Добавить типизированные DTO для auth, profile, chats, users и messages.
- [ ] Централизовать обработку ошибок, таймаутов и отмены запросов.
- [ ] Сделать обновления store иммутабельными и предсказуемыми.
- [ ] Ввести типизированные селекторы и actions.
- [ ] Снизить прямую связанность UI-компонентов с контроллерами.

### 4. UI-архитектура

- [ ] Принять решение: оставить собственный UI-фреймворк или мигрировать на поддерживаемый современный фреймворк.
- [ ] Консервативный путь: оставить `Block`, но улучшить типизацию, очистку lifecycle и рендер дочерних компонентов.
- [ ] Практичный путь: перейти на React + Vite + React Router + Zustand/TanStack Query.
- [ ] Мигрировать постранично: auth, profile, список чатов, область сообщений, модальные окна.

### 5. Редизайн

- [ ] Собрать адаптивный layout для desktop, tablet и mobile.
- [ ] Улучшить плотность списка чатов, группировку сообщений, empty/loading/error states.
- [ ] Создать небольшую дизайн-систему: кнопки, инпуты, модальные окна, dropdown, toast, аватары и message bubbles.
- [ ] Добавить доступные focus-состояния, label и навигацию с клавиатуры.
- [ ] Добавить поддержку светлой и темной темы через design tokens.
- [ ] Пересмотреть палитру, чтобы интерфейс не держался почти полностью на одной темной фиолетово-синей гамме.

### 6. Улучшение функционала

- [ ] Добавить полнотекстовый поиск по сообщениям.
- [ ] Добавить вложения с предпросмотром файлов и валидацией загрузки.
- [ ] Добавить статусы доставки и прочтения сообщений.
- [ ] Добавить индикатор набора текста и статус online/offline.
- [ ] Улучшить offline-поведение: очередь отправки и повторные попытки.
- [ ] Улучшить управление групповыми чатами: роли, список участников, приглашение/удаление и аватар чата.
- [ ] Доработать профиль: crop аватара, выбор темы и настройки уведомлений.
- [ ] Добавить инфраструктуру локализации для русского и английского интерфейса.

## Рекомендуемый порядок следующих работ

1. Спланировать и выполнить безопасные dependency upgrades.
2. Отрефакторить API-транспорт и управление состоянием.
3. Сделать редизайн текущего UI без смены фреймворка.
4. Перейти на современный UI-стек, если проект будет развиваться дальше.

## Чеклист проверки

- [x] `npm.cmd run lint` проходит.
- [x] `npm.cmd run typecheck` проходит.
- [x] `npm.cmd run build` создает production-ready bundle.
- [x] `npx.cmd webpack --config webpack.dev.js` компилирует dev-конфиг.
- [x] Приложение проверено e2e при прямом переходе на вложенные маршруты.
- [x] CSP проверен e2e: production-заголовок не содержит `unsafe-inline` и `unsafe-eval`.

## Открытые решения

- [ ] Оставлять собственный UI-фреймворк или мигрировать на React/Svelte/Vue.
- [x] Оставлять Webpack или переходить на Vite.
- [ ] Сохранять контракт API Яндекс Практикума как есть или добавить adapter layer.
- [ ] Поддерживать только русский интерфейс или сразу закладывать локализацию.
- [ ] Сначала делать редизайн или сначала модернизировать архитектуру.
