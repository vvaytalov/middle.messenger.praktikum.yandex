<!doctype html>
<html lang="ru">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       {{#if styles}} <link rel="stylesheet" href="{{styles}}">{{/if}}
        <title>{{title}}</title>
    </head>
    <body>
        {{{block "body"}}}
        {{#if script}}<script src="{{script}}"></script>{{/if}}
    </body>
</html>